# 项目目前进度

> 最后更新：2026-07-10（12 项已修复，0 项待修复 ✅）

---

## 📊 总体评估

已完成 **全部12个页面** 的 UI 框架、交互逻辑和功能缺口修复，API 层、类型系统、状态管理都已定义完整。所有已知问题已修复，后续工作集中在**对接真实后端**。

---

## ✅ 已完成的（37项）

### 前台页面（5个）

| 页面 | 路径 | 完成内容 |
|------|------|---------|
| 首页 | `src/views/Front/Index/Index.vue` | 横幅导航、功能卡片、热门推荐（API优先+mock兜底）、统计卡片、人机验证 |
| 智能问答 | `src/views/Front/ChatAI/ChatAI.vue` | 双栏布局（历史+对话）、新建/删除对话、快捷模板、流式模拟、追问按钮、收藏、自动滚动 |
| 知识图谱 | `src/views/Front/GraphBrowse/GraphBrowse.vue` | ECharts 力导向图、节点搜索/高亮/闪烁、BFS 路径查找、收藏（localStorage）、PNG 导出、实体类型筛选 |
| 病例教学 | `src/views/Front/CaseStudy/CaseStudy.vue` | 表单录入（患者信息+主诉+舌脉）、关键词匹配分析（4种证候）、迷你图谱、历史病例卡片、教学笔记 |
| 历史记录 | `src/views/Front/History/History.vue` | 三栏 Tab（对话/收藏/病例）、搜索/排序/分页、URL query 同步、骨架加载态、空态 |

### 后台管理页面（7个）

| 页面 | 路径 | 完成内容 |
|------|------|---------|
| 药材管理 | `src/views/Admin/HerbManage/HerbManage.vue` | 完整 CRUD + 22种分类搜索 + 10列表格 + Excel导入导出 + 批量删除 |
| 方剂管理 | `src/views/Admin/PresManage/PresManage.vue` | 完整 CRUD + 15部典籍来源 + 药材多选组合 + Excel导入导出 + 制法字段 |
| 症状管理 | `src/views/Admin/SymptomManage/SymptomManage.vue` | 完整 CRUD + 19种分类 + 关联证候查看弹窗 + 症状多选 + Excel导入导出 |
| 证候管理 | `src/views/Admin/SyndromeManage/SyndromeManage.vue` | 完整 CRUD + 8种辨证体系 + 核心症状/对用方剂多选 + 病机/治则字段 + Excel导入导出 |
| 图谱关系 | `src/views/Admin/RelationManage/RelationManage.vue` | 三元组 CRUD + 实体类型筛选选择器 + 8种关系类型 + 清除无效关系 + ID前缀推断 |
| 文献管理 | `src/views/Admin/DocManage/DocManage.vue` | 完整 CRUD + Markdown编辑器（markdown-it）+ 实体关联（动态增删行）+ 文件上传 + Excel导出 |
| 问答记录 | `src/views/Admin/RecordManage/RecordManage.vue` | 统计仪表盘 + 日期范围筛选 + 收藏筛选 + 详情弹窗（Markdown渲染+实体分组）+ Excel导出 |

### 基础设施

| 模块 | 路径 | 完成内容 |
|------|------|---------|
| API 层 | `src/api/index.ts` | 30+ 接口定义（chatApi / graphApi / entityApi / relationApi / caseApi / recordApi / statsApi / literatureApi / documentApi） |
| 路由 | `src/router/index.ts` | 前台 5 条 + 后台 7 条 + 通配重定向，路由守卫已注释 |
| 状态管理 | `src/store/index.ts` | 4个 Pinia Store（user / chat / graph / adminTable） |
| 类型定义 | `src/types/index.ts` | 完整实体接口（Herb / Prescription / Symptom / Syndrome / GraphRelation / Document / Record / AnswerResponse） |
| SSE 工具 | `src/utils/stream.ts` | StreamSSE 类 + simulateStreamResponse + parseSSEData |
| 验证码 | `src/utils/captcha.ts` + `CaptchaVerify.vue` | 数学加减乘除验证 + sessionStorage 会话保持 |
| 导入弹窗 | `src/components/Common/ImportDialog.vue` | Excel 拖拽上传 + 模板下载 + 结果展示 |
| 聊天气泡 | `src/components/Chat/ChatBubble.vue` | Markdown 渲染（markdown-it）+ 展开/收起 + 复制 + 语音播放 + 追问按钮 |

---

## 📋 第一轮已修复（7 项，2026-07-10）

| # | 修复项 | 涉及文件 |
|---|--------|---------|
| 1 | ChatAI 接入真实 API（三级兜底：`chatApi.askQuestion()` → SSE `StreamSSE` → mock） | `ChatAI.vue` |
| 2 | CaseStudy 证候分析扩展（4 种 → 10 种证候） | `CaseStudy.vue` |
| 3 | Index 统计改用 `statsApi` + 推荐点击跳转图谱自动搜索 | `Index.vue` |
| 4 | 症状/证候管理加 Excel 导入（ImportDialog + API 层 import 方法） | `SymptomManage.vue` / `SyndromeManage.vue` / `api/index.ts` |
| 5 | 症状/证候/文献管理加导出（Blob 下载 + API 层 export 方法） | 3 个 Admin 页 + `api/index.ts` |
| 6 | PresManage 表单补上制法字段（全链路贯通） | `PresManage.vue` |
| 7 | DocManage Markdown 渲染统一为 `markdown-it`（与 ChatBubble 一致） | `DocManage.vue` |

---

## 📋 第二轮已修复（5 项，2026-07-10）

| # | 修复项 | 位置 | 说明 |
|---|--------|------|------|
| 1 | **GraphBrowse 节点居中** | `GraphBrowse.vue` | 重写 `centerOnNode`：通过 `GraphCoordinateSystem.setPan()` 正确平移视图，900ms 延迟等力导向布局完成，闪烁在居中后触发 |
| 2 | **ChatAI 历史服务端持久化** | `ChatAI.vue` + `api/index.ts` | 新增 `saveHistory` / `getHistoryDetail` API；`sendMessage` 每次对话同步写 API + localStorage；`loadHistory` 三级兜底（API → localStorage → 提示）；`deleteHistory`/`newChat`/`clearChat` 联动清理 |
| 3 | **语音播放集成到页面** | `ChatAI.vue` | 输入栏新增全局"朗读"按钮，一键朗读最新 AI 回答；与 ChatBubble 逐条按钮形成互补 |
| 4 | **后台实体详情跳转** | `GraphBrowse.vue` + 4 个 Admin 页 | `goToAdminDetail` 路由携带 `?editId=xxx`；4 个管理页 `onMounted` 读取 query 并自动打开编辑弹窗 |
| 5 | **历史记录批量操作** | `History.vue` | 收藏和病例卡片加 checkbox + 全选；批量删除（含确认弹窗 + localStorage 同步）；CSV 导出（支持全量/选中导出，BOM 头兼容 Excel 中文） |

---

## 🗑️ 保留不动（无需修复）

| 项目 | 原因 |
|------|------|
| 3 个 composable 空壳（`useChat.ts` / `useGraph.ts` / `useTableCRUD.ts`） | 页面内联逻辑已足够，抽取可能引入问题 |
| 7 个未使用的死代码文件 | 预留扩展用，清理可能导致引用报错 |
| Layout CSS margin 异常值（`1100px`） | 当前负 margin 补救可用，调整可能引发连锁布局问题 |
| AdminLayout 拼写错误（`atuo` → `auto`） | 浏览器容错，修改无实质收益 |
