📊 总体评估
你已完成 全部12个页面的 UI 框架和交互逻辑，API 层、类型系统、状态管理都已定义完整。剩余工作主要集中在对接真实后端和补齐部分功能缺口。

✅ 已完成的（37项）
前台页面（5个）
首页 — 横幅导航、功能卡片、热门推荐（含mock兜底）、统计卡片、人机验证
智能问答 — 双栏布局、对话历史、快捷模板、流式模拟、追问按钮、收藏
知识图谱 — ECharts力导向图、节点搜索/高亮、路径查找BFS、收藏、PNG导出
病例教学 — 表单录入、关键词匹配分析、迷你图谱展示、历史病例卡片
历史记录 — 三栏Tab、搜索/排序/分页、骨架加载态、空态
后台管理页面（7个）
药材管理 — 完整CRUD + 22种分类 + Excel导入导出
方剂管理 — 完整CRUD + 15部典籍来源 + 药材多选组合
症状管理 — 完整CRUD + 19种分类 + 关联证候查看
证候管理 — 完整CRUD + 8种辨证体系 + 症状/方剂多选
图谱关系 — 三元组CRUD + 实体类型筛选 + 清除无效关系
文献管理 — Markdown编辑器 + 实体关联 + 文件上传
问答记录 — 统计仪表盘 + 日期筛选 + 详情弹窗 + 导出
基础设施
API层 30+ 接口定义、4个 Pinia Store、完整 TypeScript 类型、SSE 流式工具、验证码组件
⚠️ 部分完成的（11项）
问题	位置	说明
ChatAI 用假数据	ChatAI.vue	sendMessage 永远返回失眠相关的硬编码回答，未调 chatApi
CaseStudy 假分析	CaseStudy.vue	关键词匹配代替 Agent 分析，仅支持4种证候
症状/证候 缺Excel导入	SymptomManage / SyndromeManage	药材和方剂有导入，这两个没有
症状/证候/文献 导出未实现	3个Admin页	显示"导出功能开发中"
Index 统计用 raw fetch	Index.vue	应改用 statsApi.getPlatformStats()
Index viewDetail 是空函数	Index.vue	推荐列表点击只是 console.log
GraphBrowse 节点居中不准确	GraphBrowse.vue	使用了 ECharts center 选项但坐标计算有误
PresManage 缺少制法字段	PresManage.vue	表单中 preparation 写死为空字符串
3个 composable 是空壳	useChat/useGraph/useTableCRUD	功能直接写在页面里，composable 未抽取
❌ 缺失的（8项）
问题	说明
ChatAI 未接真实 SSE	chatApi.askQuestionStream 定义了 EventSource 但从没调用
语音播放未集成到页面	ChatBubble 组件里有 speechSynthesis，但 ChatAI 页面没传控制入口
认证体系空白	路由守卫注释掉了，login() / register() 只有 console.log
后台实体详情跳转缺失	GraphBrowse 右侧面板"查看详情"只跳到列表页，没跳到具体编辑弹窗
Index 推荐无实体详情跳转	热门药材/方剂点击无反应
Layout CSS bug	FrontLayout .main-content 有 margin: 1100px auto 0，子页面用负 margin 补救
🗑️ 死代码（7个文件）
这些文件已完整编写但没有被任何页面引用，可以后续抽取整合：

src/composables/useChat.ts — 聊天逻辑空壳
src/composables/useGraph.ts — 图谱逻辑空壳
src/composables/useTableCRUD.ts — 表格CRUD逻辑空壳
src/components/Graph/GraphVisualization.vue — 图谱画布组件未用
src/components/Graph/GraphNodeDetail.vue — 节点详情组件未用
src/components/Chat/ChatInput.vue — 340行完整代码但未用
src/components/Common/BaseTable.vue — 326行通用表格但未用
🎯 建议优先级
高优先级（功能缺口）：

ChatAI 接入真实 SSE 流式问答
CaseStudy 接入 caseApi.analyze()
Index 统计数据改用 statsApi + 推荐项点击跳转详情
中优先级（补齐功能）：
4. 症状/证候管理加 Excel 导入
5. 症状/证候/文献管理加导出
6. PresManage 加制法字段

低优先级（代码整理）：
7. 把页面内联逻辑抽取到 composable / 通用组件
8. 修复 Layout CSS 的 margin 问题
9. 清理死代码或将其整合进页面